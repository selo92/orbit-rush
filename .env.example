# Leave empty so the game calls /api on the same origin
# (Vite dev proxy locally, Cloudflare Worker in production).
# Set only when the API is hosted on a different origin. No trailing slash.
VITE_API_BASE=
